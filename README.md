
<h1 align="center">Remna-install</h1>

<p align="center">
  <b>Автоматический установщик Remnawave Panel + Node с NGINX reverse proxy и XRAY Reality</b>
</p>

<p align="center">
  <a href="https://github.com/DanteFuaran/Remna-install/releases"><img src="https://img.shields.io/github/v/release/DanteFuaran/Remna-install?style=flat-square&color=blue" alt="Release"></a>
  <a href="https://github.com/DanteFuaran/Remna-install/blob/main/LICENSE"><img src="https://img.shields.io/github/license/DanteFuaran/Remna-install?style=flat-square&color=green" alt="License"></a>
  <a href="https://github.com/DanteFuaran/Remna-install/stargazers"><img src="https://img.shields.io/github/stars/DanteFuaran/Remna-install?style=flat-square&color=yellow" alt="Stars"></a>
</p>

---

## 📖 Описание

Скрипт автоматизирует полное развёртывание **[Remnawave](https://github.com/remnawave/backend)** — панели управления VPN-прокси на базе XRAY. Установщик настраивает NGINX reverse proxy, XRAY VLESS Reality, SSL-сертификаты, Docker-контейнеры и файрвол — всё в одну команду.

### Архитектура

```
Клиент → :443 (XRAY VLESS Reality) → Unix Socket → NGINX → Remnawave Panel / Subscription / SelfSteal
```

XRAY слушает на порту 443 и обрабатывает TLS. Авторизованный VPN-трафик маршрутизируется напрямую. Остальной (браузерный) трафик передаётся через Unix socket (`/dev/shm/nginx.sock`) на NGINX, который проксирует запросы по доменам.

---

## ✨ Возможности

| Возможность | Описание |
|---|---|
| 🖥️ **3 режима установки** | Панель + Нода, только Панель, только Нода |
| 🔐 **SSL-сертификаты** | Cloudflare DNS-01 (wildcard) и ACME HTTP-01 (Let's Encrypt) |
| 🌐 **XRAY VLESS Reality** | Передовая маскировка трафика |
| 🎭 **SelfSteal шаблоны** | Случайные HTML-заглушки для маскировки |
| 🐳 **Docker Compose** | Автоматическая оркестрация всех сервисов |
| 🔥 **UFW Firewall** | Автонастройка файрвола |
| 🚀 **BBR** | Оптимизация TCP congestion control |
| 🔄 **Авто-обновление** | Обновление панели, ноды и самого скрипта |
| 🔑 **Рандомные секреты** | Уникальная генерация `WEBHOOK_SECRET_HEADER` при каждой установке |
| 📋 **Управление** | Запуск, остановка, логи, переустановка через меню |
| 🎯 **Навигация стрелками** | Удобное интерактивное меню с анимированными спиннерами |

---

## 📋 Требования

### Операционная система

| ОС | Версии |
|---|---|
| **Debian** | 11 (Bullseye), 12 (Bookworm) |
| **Ubuntu** | 22.04 (Jammy), 24.04 (Noble) |

### Минимальные требования

- **CPU:** 1 ядро
- **RAM:** 1 ГБ
- **Диск:** 10 ГБ
- **Права:** root
- **Порт 443:** свободен

### Домены

Для полной установки (Панель + Нода) требуется **3 домена** (или поддомена):

| Домен | Назначение | Пример |
|---|---|---|
| **Панель** | Веб-интерфейс управления | `panel.example.com` |
| **Подписка** | Страница подписки клиентов | `sub.example.com` |
| **SelfSteal** | Заглушка / домен ноды | `node.example.com` |

> ⚠️ Все домены должны указывать (A-запись) на IP вашего сервера. При использовании Cloudflare — **отключите проксирование** (серое облако, DNS Only).

---

## 🚀 Быстрый старт

### Установка одной командой

```bash
bash <(curl -Ls https://raw.githubusercontent.com/DanteFuaran/Remna-install/refs/heads/main/install_remnawave.sh)
```

### Повторный запуск

После установки скрипт доступен как команда:

```bash
remna_install
```

Или короткий алиас:

```bash
ri
```

---

## 📦 Режимы установки

### 1. Панель + Нода (один сервер)

Полная установка всех компонентов на одном сервере. Идеально для начала.

**Что устанавливается:**
- Remnawave Backend (панель)
- PostgreSQL 17 (база данных)
- Valkey 9.0 (Redis-совместимый кэш)
- NGINX 1.28 (reverse proxy)
- Remnawave Subscription Page (страница подписки)
- Remnawave Node + XRAY (VPN-нода)

**Требуется:** 3 домена

### 2. Только панель

Установка панели без VPN-ноды. Ноды подключаются с других серверов.

**Что устанавливается:**
- Remnawave Backend
- PostgreSQL 17
- Valkey 9.0
- NGINX 1.28
- Remnawave Subscription Page

**Требуется:** 2 домена (панель + подписка)

### 3. Только нода

Установка VPN-ноды для подключения к существующей панели.

**Что устанавливается:**
- Remnawave Node + XRAY
- NGINX 1.28 (SelfSteal)

**Требуется:** 1 домен (SelfSteal) + IP панели + сертификат (SECRET_KEY) из панели

---

## 🔐 SSL-сертификаты

### Метод 1: Cloudflare DNS-01 (рекомендуется)

Получает **wildcard-сертификат** (`*.example.com`). Один сертификат покрывает все поддомены.

**Требования:**
- Домен на Cloudflare
- Cloudflare API Token с правами `Zone:DNS:Edit`

**Как получить API Token:**
1. Зайдите в [Cloudflare Dashboard](https://dash.cloudflare.com/profile/api-tokens)
2. **Create Token** → **Edit zone DNS** (шаблон)
3. Выберите нужную зону
4. Скопируйте токен

### Метод 2: ACME HTTP-01 (Let's Encrypt)

Получает отдельный сертификат для каждого домена. Временно открывает порт 80.

**Требования:**
- Порт 80 должен быть свободен во время получения сертификата
- Каждый домен должен указывать на сервер

---

## 🌐 Настройка DNS

### Один сервер (Панель + Нода)

| Тип | Имя | Значение | Прокси |
|---|---|---|---|
| A | `panel` | `IP сервера` | ❌ DNS Only |
| A | `sub` | `IP сервера` | ❌ DNS Only |
| A | `node` | `IP сервера` | ❌ DNS Only |

### Распределённая установка

**Сервер панели:**

| Тип | Имя | Значение | Прокси |
|---|---|---|---|
| A | `panel` | `IP сервера панели` | ❌ DNS Only |
| A | `sub` | `IP сервера панели` | ❌ DNS Only |

**Сервер ноды:**

| Тип | Имя | Значение | Прокси |
|---|---|---|---|
| A | `node` | `IP сервера ноды` | ❌ DNS Only |

---

## ⚙️ Конфигурация

### Файлы конфигурации

После установки все файлы находятся в `/opt/remnawave/`:

| Файл | Описание |
|---|---|
| `.env` | Переменные окружения (JWT, БД, Webhook и др.) |
| `docker-compose.yml` | Конфигурация Docker-сервисов |
| `nginx.conf` | Конфигурация NGINX |

### Основные переменные .env

| Переменная | Описание |
|---|---|
| `JWT_AUTH_SECRET` | Секрет для авторизации (генерируется автоматически) |
| `JWT_API_TOKENS_SECRET` | Секрет для API токенов (генерируется автоматически) |
| `WEBHOOK_SECRET_HEADER` | Секрет вебхука (генерируется случайно каждый раз) |
| `FRONT_END_DOMAIN` | Домен панели |
| `SUB_PUBLIC_DOMAIN` | Домен подписки |
| `DATABASE_URL` | Строка подключения к PostgreSQL |

---

## 🛠️ Управление

### Меню управления

После установки запустите `remna_install` или `ri` для доступа к меню:

```
════════════════════════════════════════
   🚀 REMNAWAVE INSTALLER v1.0.0
════════════════════════════════════════

▶ 📦  Установить компоненты
  🔄  Обновить панель/ноду
  ▶️   Запустить сервисы
  ⏹️   Остановить сервисы
  📋  Просмотр логов
  🎨  Случайный шаблон selfsteal
  🔄  Переустановить
  🔄  Обновить скрипт
  🗑️   Удалить скрипт
  ❌  Выход

════════════════════════════════════════
Используйте ↑↓ для навигации, Enter для выбора
```

### Docker-команды (ручное управление)

```bash
# Статус контейнеров
cd /opt/remnawave && docker compose ps

# Логи всех сервисов
cd /opt/remnawave && docker compose logs -f

# Логи конкретного сервиса
cd /opt/remnawave && docker compose logs -f remnawave
cd /opt/remnawave && docker compose logs -f remnawave-nginx
cd /opt/remnawave && docker compose logs -f remnanode

# Перезапуск
cd /opt/remnawave && docker compose restart

# Остановка
cd /opt/remnawave && docker compose down

# Обновление
cd /opt/remnawave && docker compose pull && docker compose up -d
```

---

## 🐳 Docker-сервисы

| Сервис | Образ | Порт | Описание |
|---|---|---|---|
| `remnawave` | `remnawave/backend:2` | 3000 | Панель управления |
| `remnawave-db` | `postgres:17` | 6767 | База данных |
| `remnawave-redis` | `valkey/valkey:9.0.0-alpine` | 6379 | Кэш |
| `remnawave-nginx` | `nginx:1.28` | host | Reverse proxy |
| `remnawave-subscription-page` | `remnawave/subscription-page` | 3010 | Страница подписки |
| `remnanode` | `remnawave/node` | 2222, host | VPN-нода (XRAY) |

---

## 🔒 Безопасность

- **UFW Firewall** — только порты 22 (SSH) и 443 (HTTPS) открыты
- **IPv6 отключён** — снижение attack surface
- **BBR** — оптимизация TCP
- **SSL/TLS 1.2+** — современные шифры
- **XRAY Reality** — маскировка VPN-трафика под обычный HTTPS
- **SelfSteal** — на домене ноды отображается случайный сайт-заглушка
- **Рандомные секреты** — JWT, webhook secret генерируются уникально при каждой установке
- **Автообновление сертификатов** — cron-задача для `certbot renew`

---

## 🔄 Обновление

### Обновление панели и ноды

Через меню скрипта: **«Обновить панель/ноду»** — автоматически выполнит `docker compose pull` и перезапустит сервисы.

### Обновление скрипта

Через меню: **«Обновить скрипт»** — загрузит последнюю версию из этого репозитория.

Или вручную:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/DanteFuaran/Remna-install/refs/heads/main/install_remnawave.sh)
```

---

## ❓ Решение проблем

### Панель не открывается

```bash
# Проверьте статус контейнеров
cd /opt/remnawave && docker compose ps

# Проверьте логи
cd /opt/remnawave && docker compose logs remnawave

# Проверьте NGINX
cd /opt/remnawave && docker compose logs remnawave-nginx
```

### Нода не подключается к панели

1. Убедитесь что порт 2222 открыт в файрволе для IP панели
2. Проверьте SECRET_KEY в docker-compose.yml ноды
3. Проверьте логи: `docker compose logs remnanode`

### Ошибка сертификата

```bash
# Проверьте сертификаты
certbot certificates

# Обновите вручную
certbot renew --force-renewal
cd /opt/remnawave && docker compose restart remnawave-nginx
```

### Порт 443 занят

```bash
# Найти процесс на порту
ss -tlnp | grep 443

# Остановить Apache если он запущен
systemctl stop apache2 && systemctl disable apache2
```

---

## 📁 Структура проекта

```
Remna-install/
├── install_remnawave.sh    # Основной скрипт установки
├── README.md               # Документация
└── LICENSE                 # MIT лицензия
```

### Структура после установки

```
/opt/remnawave/
├── .env                    # Переменные окружения
├── docker-compose.yml      # Docker Compose конфигурация
├── nginx.conf              # NGINX конфигурация
└── remna_install           # Копия скрипта

/var/www/html/              # SelfSteal шаблон
/usr/local/bin/remna_install # Симлинк на скрипт
/etc/letsencrypt/           # SSL-сертификаты
```

---

## 📜 Лицензия

Этот проект распространяется под лицензией [MIT](LICENSE).

---

## 🙏 Благодарности

- [Remnawave](https://github.com/remnawave) — панель управления
- [XRAY-core](https://github.com/XTLS/Xray-core) — ядро прокси
- [eGames](https://github.com/eGamesAPI/remnawave-reverse-proxy) — вдохновение для скрипта

---

<p align="center">
  <b>⭐ Если проект полезен — поставьте звезду!</b>
</p>
